{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_TEST_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_PROD_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  }
}
